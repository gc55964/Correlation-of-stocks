<!DOCTYPE html>
<html>
<head>
    <title>Stock Correlation</title>
</head>
<body>
    <h1>Stock Correlation Calculator</h1>
    <form method="POST" action="/correlate">
        <input name="stock1" placeholder="First Stock Symbol" required>
        <input name="stock2" placeholder="Second Stock Symbol" required>
        <button type="submit">Calculate</button>
    </form>

    <% if (correlation !== null) { %>
        <h3>Correlation: <%= correlation.toFixed(4) %></h3>
    <% } %>

    <% if (error) { %>
        <p style="color:red;"><%= error %></p>
    <% } %>
</body>
</html>
